#include <stdio.h>

int main (void) {
    int nums [4];
    char numc [50];

    printf("Enter all the numbers from 1 - 16 in any order: ");
    scanf("%s", numc);

    printf("\"%s\"\n", numc);

    return 0;
}
